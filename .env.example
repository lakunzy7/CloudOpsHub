# ── Database ──
DB_ROOT_PASSWORD=changeme
DB_PASSWORD=changeme
DATABASE_URL=mysql://appuser:changeme@database:3306/bookstore

# ── App ──
NODE_ENV=development
PORT=8080

# ── Monitoring ──
GRAFANA_ADMIN_PASSWORD=changeme
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/placeholder

# ── Registry (production only) ──
# REGISTRY=us-central1-docker.pkg.dev/your-project/cloudopshub-docker
# GITOPS_ENV=dev
